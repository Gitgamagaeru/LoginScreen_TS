# 【実践チートシート】AIと協働する具体的フロー（Issue #2 の実例）

先ほどの Issue #2 で実際に行った「AIをアシスタントとしてフル活用する」ための最も効率的なハンズオン手順です。日常の開発はこの手順に沿って進めてください。

### Step 1: ブランチ作成と実装
1. `git checkout -b feature/#<番号>-<機能名>` (例: `feature/#3-google-login`) のように新しい作業ブランチを作る。
2. AI（私）と一緒に実装・問題解決を行う（未コミットのままでOK）。

### Step 2: AIによる影響度調査・ドキュメント自動監査
1. チャットで **「影響度を調査して」** と指示。
   - AIが自動で `LOW/MID/HIGH` を評価し、必要なら `ADR` を生成します。
2. チャットで **「validate-docsを実施して」** と指示。
   - AIがドキュメントの矛盾や日付のズレを監査します。「修正して」と頼めば、関連ドキュメントをすべて最新化してくれます。

### Step 3: コミットとコードレビュー
1. コマンドラインやVSCodeからコミットします。
   - ⚠️ 注意: `commitlint` のエラーを回避するため、必ず `feat(auth): Googleログインの実装` のように英語の小文字や日本語から開始してください。
2. チャットで **「PR Review をお願い」** と指示。
   - AIがタイポや思わぬバグ（例: セッション変数の渡し忘れ）などを指摘します。指摘事項は「修正して」と頼めば、AIが自動修正して追加コミットまで行います。

### Step 4: PR作成と合流（GitHub）
1. VS Code左側の青い「Branchの発行（Push）」ボタンを押す。
2. GitHub画面を開き、PRを作成。説明文に **`Closes #<番号>`** （魔法のキーワード）を記載する。
3. 問題なければ、緑のボタンの **「Create a merge commit」** を選んで `main` ブランチに合流させる（これにより履歴が途切れず、作業ブランチが `main` に繋がります）。

### Step 5: AIによる全自動の事後（クリーンアップ）処理
1. チャットで **「やった」「マージできたよ」** とAIに報告する。
2. 私の方で自動的に `main` ブランチへの移動・最新化（Pull）、古いブランチの削除、`TODO.md` のチェックオフ、`CHANGELOG.md` への追記、そしてそれらのコミットまで全て代行します！
